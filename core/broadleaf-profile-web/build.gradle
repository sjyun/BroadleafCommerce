
description = 'BroadleafCommerce Profile Web'
dependencies {
  compile project(':core:broadleaf-profile')
    compile group: 'org.springframework', name: 'spring-webmvc', version:'3.2.12.RELEASE'
    compile group: 'jstl', name: 'jstl', version:'1.1.2'
    compile group: 'taglibs', name: 'standard', version:'1.1.2'
    compile(group: 'org.springframework.security', name: 'spring-security-acl', version:'3.1.3.RELEASE') {
exclude(module: 'spring-core')
exclude(module: 'spring-expression')
exclude(module: 'spring-tx')
exclude(module: 'spring-jdbc')
exclude(module: 'spring-aop')
    }
    compile(group: 'org.springframework.security', name: 'spring-security-taglibs', version:'3.1.3.RELEASE') {
exclude(module: 'spring-core')
exclude(module: 'spring-expression')
exclude(module: 'spring-tx')
exclude(module: 'spring-jdbc')
exclude(module: 'spring-aop')
    }
    compile(group: 'org.springframework.security', name: 'spring-security-config', version:'3.1.3.RELEASE') {
exclude(module: 'spring-aop')
    }
    runtime group: 'org.springframework', name: 'spring-oxm', version:'3.2.12.RELEASE'
    runtime group: 'com.thoughtworks.xstream', name: 'xstream', version:'1.3.1'
    compile(group: 'javax.servlet', name: 'servlet-api', version:'2.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'javax.servlet.jsp', name: 'jsp-api', version:'2.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
