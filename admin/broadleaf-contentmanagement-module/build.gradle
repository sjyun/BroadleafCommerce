
description = 'BroadleafCommerce CMS Module'
dependencies {
  compile project(':admin:broadleaf-open-admin-platform')
    compile group: 'org.mvel', name: 'mvel2', version:'2.2.0.Final'
    compile group: 'org.thymeleaf', name: 'thymeleaf', version:'2.1.3.RELEASE'
    compile group: 'org.thymeleaf', name: 'thymeleaf-spring3', version:'2.1.3.RELEASE'
    compile group: 'org.javassist', name: 'javassist', version:'3.17.1-GA'
    compile group: 'net.sf.ehcache', name: 'ehcache', version:'2.7.2'
  testCompile project(':broadleaf-common')
    testCompile group: 'org.easymock', name: 'easymock', version:'2.5.1'
    testCompile group: 'junit', name: 'junit', version:'4.11'
    testCompile group: 'org.springframework', name: 'spring-test', version:'3.2.12.RELEASE'
    compile(group: 'javax.servlet', name: 'servlet-api', version:'2.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'javax.servlet.jsp', name: 'jsp-api', version:'2.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
